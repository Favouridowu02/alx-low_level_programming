#include "main.h"
#include <stdio.h>

/**
 * _abs - Write a function that computes the absolute
 * value of an integer.
 * Return: returns the absolute value of aan interger
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
