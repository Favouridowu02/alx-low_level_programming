#include "main.h"

/**
 * add - adds two integers and returns the result
 * @a: the first parameter to be passed
 * @b: the second parameterr to be passed
 * Return: returns the result
 */
int add(int a, int b)
{
	int result = a + b;

	return (result);
}
